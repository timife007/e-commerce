package com.timife.repositories;

//import com.timife.model.entities.ShopItem;
//import org.springframework.data.jpa.repository.JpaRepository;
//import org.springframework.data.jpa.repository.Query;
//
//public interface ShopItemRepository extends JpaRepository<ShopItem, Long> {
//
//    @Query("SELECT * FROM shop_item u WHERE u.name = ?1 AND u.category.id = ?2 AND u.subCategory.id = ?3 AND u.gender.id = ?4")
//    ShopItem findByIdAndSubCategory(String name, Long categoryId, Long subcategory, Long genderId);
//}
